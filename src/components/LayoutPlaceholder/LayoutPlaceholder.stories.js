import React from "react";

import LayoutPlaceholder from "./LayoutPlaceholder";

export default {
  component: LayoutPlaceholder,
  title: "LayoutPlaceholder",
};

export const Default = (args) => <LayoutPlaceholder {...args} />;
